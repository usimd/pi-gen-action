name: Shellcheck
on: push

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: run shellcheck
        uses: ludeeus/action-shellcheck@master
        with:
          severity: error
