name: Run pi-gen
on: push

jobs:
  pi-gen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          image-name: test
          custom-stage: test
          release: buster

